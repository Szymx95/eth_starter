# This allows for this plugin to also lint your configuration files inside the .storybook folder,
# so that you always have a correct configuration and don't face any issues regarding mistyped addon names, for instance.
# See: https://github.com/storybookjs/eslint-plugin-storybook#installation
!.storybook
**/tailwind